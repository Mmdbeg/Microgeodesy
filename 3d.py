# +-+-+-+-+-+ 3d observation V angles +-+-+-+-+-+-+-

# l0_V = sp.Matrix([index(df, i, 'V_corrected') for i in range(len(df))])

# l_s_v = sp.zeros(len(df),1)   # 

# for i in range(len(df)):
#     a = index(df,i,'FROM')
#     b = index(df,i,'TO')
#     l_s_v[i] = f"{a}_{b}"

from func_dep import *
 

